# Railway / API only — FastAPI + Snowflake + query-time RAG (no ingestion)

fastapi==0.135.1
uvicorn==0.42.0
pydantic==2.12.5

# Snowflake + key-pair auth
snowflake-connector-python==4.3.0
cryptography==46.0.5

# LLM (agentic RAG)
google-genai==1.68.0

# Query-time: chunk embedding + sci NER for graph_search (backend/retrieval.py)
sentence-transformers==5.3.0
spacy==3.7.5
scispacy==0.6.2
en-core-sci-sm @ https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.4/en_core_sci_sm-0.5.4.tar.gz

python-dotenv==1.2.2
